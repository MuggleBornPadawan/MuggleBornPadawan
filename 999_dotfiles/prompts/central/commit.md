Analyze the staged changes (`git diff --cached`). If nothing is staged, use the working tree diff and tell me.

Then:
1. Summarize what changed and why in one sentence
2. Propose a conventional commit message: `type(scope): short imperative summary`
3. Add a body only if the change is non-trivial (what + why, not how)
4. Show me the message and ask for confirmation before committing
