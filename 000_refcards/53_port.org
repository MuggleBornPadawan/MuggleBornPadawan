#+TITLE: port 53 - reroute content later to appropriate sections 
#+OPTIONS: toc:2
* Table of Contents :TOC:
- [[#trivia][trivia]]
  - [[#random-notes][random notes]]
  - [[#postgresql---full-stack][postgresql - full stack]]
  - [[#self-hosting][self hosting]]
  - [[#linux-file-directories][linux file directories]]
  - [[#aihero---matt-pocock][aihero - matt pocock]]
  - [[#rfc][rfc]]
  - [[#quant-funds][quant funds]]
  - [[#c-cultrep][c++ cult,rep]]
  - [[#java-story---cultrepo][java story - cult.repo]]
  - [[#dhh-vs-rich-hickey][dhh vs rich hickey]]
  - [[#clojure-philosophy][clojure philosophy]]
  - [[#clojure-reitit-vs-compojure][clojure reitit vs compojure]]
  - [[#clojure-equivalent-of-ruby-on-rails][clojure equivalent of ruby on rails]]
  - [[#clojure-equivalent-of-j2ee][clojure equivalent of j2ee]]
  - [[#clojure-equivalent-of-spring][clojure equivalent of spring]]
  - [[#sqlite][sqlite]]
  - [[#learn-to-build---agy][learn to build - agy]]
  - [[#genai-code-limitations][genai code limitations]]
- [[#foundational-programming-concepts][foundational programming concepts]]
- [[#millenium-problems][millenium problems]]
- [[#chronological-intellectual-evolution-table---the-staircase-to-knowledge][chronological intellectual evolution table - the staircase to knowledge]]
  - [[#western][western]]
  - [[#indian-staircase-of-knowledge][indian staircase of knowledge]]
- [[#type-systems][type systems]]
  - [[#intro][intro]]
  - [[#notes][notes]]
- [[#linus-torvalds-vs-rich-hickey][linus torvalds vs rich hickey]]
- [[#entropy---yt-video---3b1b][entropy - yt video - 3b1b]]
  - [[#compresssion--prediction--intelligence][compresssion = prediction | intelligence]]
  - [[#ai-infrastructure][ai infrastructure]]
  - [[#softwar-engineering][softwar engineering]]
- [[#github-outage][github outage]]

* trivia
** random notes
- techniques to shrink a language model: quantization, pruning, distillation 
  - https://open.substack.com/pub/bytebytego/p/how-to-shrink-a-language-model-without-295?
- `code text` -> `tree-sitter` (fast colors, concrete syntax tree) -> `abstract syntax tree` (logic map) -> `language-server-processor` (smart tips) -> `intermediate-representation` (machine steps)
- state of observer (stephen wolfram) 
- computational irreduciblity
- language - paradigms (oops, procedural, functional), abstraction level (machine code, assembly, low level, high level, mark up), translation (interpreted, compiled)
- system design order: basics → networking → load balancing → storage → async → reliability
  - minimize network round-trips (rtt) and payload size whenever possible
- emacs coq agda - axiomatic programming - proof assistant
- sovereign semantic substrate = alpha moat
- electron instance
  - vs code, slack, discord, spotify, whatsapp desktop, notion, and microsoft teams
  - html, css, and javascript
  - chromium, node.js
** postgresql - full stack 
- free, open source and 30 years - an incredibly battle-tested tech
- deploy, monitor, backup and keep in sync - a single postgresql instance than multiple systems
- typical stack: database, document store, searchh, job queue, cache, analytics
- jsonb column type - stores json document in binary format - custom attributes (mangodb)
- gin index - generalized inverted index 
- tsvector - human language search - stem words, rank, lanuguage 
- background job queue
  - jobs and background workers
  - jobs (avoid conflict by SELECT FOR UPDATE SKIP LOCKED)
  - workers can poll - eg: email users about id creation
  - listen and notify - pub/sub (redis, rabbitmq) 
- pgvector - recommendation engines - similarity search and regular business logic in single query 
- native graph queries - multi hop pattern matching
- acid compliant - all (fulfill) or nothing (roll back) transaction 
  - atomicity, consistent, isolation and durablity 
- core engine - extension system and type systems 
- client server architecture
- the oversized attribute storage technique (toast) - 8k / page
- gif compression - pglz - variant of lempel-ziv-welch (lzw) - lossless data compression algorithm for image formats like gif, tiff, pdf files
- out of line storage - toast 2kb per page
- write ahead logging (wal) and durablity - crash recovery
- bytea - blob - binary large object 
- transactions and multi-version concurrency control (mvcc) 
- vaccum
- downsides: real physical limits
  - write scaling - no native sharding
  - connections - pgbouncer 
  - dead rows - mvcc - garbage collection
** self hosting
- proxmox, nextcloud, jellyfin, immich, vaultwarden, home assistant 
** linux file directories
- file directories
  - bin, sbin, etc, var, lib, dev, boot
  - home, root, tmp, var, media, xxt, proc
- home - user files
- bin - keeps the programmes. bin stands for binaries; toolbox
  - ls, cp, mv, cat, mkdir
- sbin - system binaries - administrative
  - networking - ip, ifconfig, route
  - disk management - fdisk, mkfs, parted
  - boot - grub-install, init
  - system recovery - fsck, e2fsck
- etc - editable text configuration - network, ssh, dns
- usr - unix system resources 
- lib - shared libraries of applications
- tmp - temporary files - scratch space, wiped on reboot
- var - changes frequently - logs, queues, mail, lib, spool, databases and cache files 
- media - for external storage devices automatically mounted
- mnt - for external storage devices manually mounted (administrators) 
- dev - hardware files - hard drives, usb, keyboards, mice, printers, terminals
- proc - system information - live dashboard - virtual file system - live information - processes, memory, 
- boot - kernel, bootloader files, startup configuration
** aihero - matt pocock
- rediscover software fundamentals - https://www.aihero.dev/ 
- human in the loop (hitl)
- away from keyboard (afk) 
- ralph wiggum loop (sequential) > sandcastle (parallel) 
- flow: idea > research (optional) > prototype (optional) > prd > kanban > implementation > qa / code review 
- intentional about modules (blocks with interfaces and test boundaries) and shape of code bases
- skills 
  - /grill-me (hitl)
  - /to-spec or /to-prd (afk, doc rot will happen - remove the documents after code generation)
  - /to-issues or /to-ticket (afk, doc rot will happen - close issues in github issues so that the documents are removed)
    - tracer bullets - each ticket is a thin vertical slice cutting through all integration layers, not a horizontal slice of one layer. better feedback loop at feature level because of verticals instead of horizontal stage (/slices) coding 
    - blocking relationships to be established
  - /tdd (hitl)
  - /improve-codebase-architecture 
- bullets: systems have layers > llms code horizontally > use tracer bullets for vertical slicing of features
- feeback loop - test in smart zone (~100k), ci must stay green
- implementing coding standards?
  - push <> reviewer
  - pull <> implementer
- agents
  - opus - reviewer
  - sonnet - coding
- parallelization is missing - some sand? library was there 
** rfc
- request for comments
- rfc 822 (published in 1982) is the foundational standard for arpa internet text messages (email)
- obsoleted by rfc 2822, and currently updated by rfc 5322
** quant funds
jane street, hudson river trading, citadel securities, jump trading, xtx markets, two sigma, d. e. shaw, optiver, imc trading, drw, tower research, akuna capital, sig, millennium, worldquant, graviton research, nk securities, alphagrep, quadeye, trexquant
** c++ cult,rep
- american telephone and telegraph company (at&t) bell labs, c with classes, simula 
** java story - cult.repo
- jdk ⊃ jre ⊃ jvm
- java runtime environment (jre) = java virtual machine (jvm, execution engine) + Libraries
- java development kit (jdk)  = JRE + Tools (=javac=, debugger)
- oak - compatible with devices outside traditional industry scope (thermostat, oven, ...) 
- mosaic (web browser) - static websites to interactive models - hot java browser
- write once run anywhere
- servlets, applets, java beans 
- java 1 event
- microsoft vs java - internet explorer 4.0 had a modified version of java
- microsoft on java: embrace, extend, extinguish 
- netscape - mozilla 
- j2ee, hibernate, struts, spring, 
- j2ee -> corba influence- eclipse foundation (red hat, sun, fujitsu?) 
- java community process 
- sun microsystem -> dotcom bubble (ibm was supposed to acquire) -> oracle larry ellison acquired
- openjdk 
- java vs google android api - google won as fair use (implementation was done by them while they reused java api's headers / parameters)
- apache tomcat - open source 
- release cadence of 6 months from 4 / 5 years 
- singe by the hinge, hard by the yard - meaning
- straw man proposal
- panama - foreign functions and foreign data
- amber - high productivity language features
- loom - lightweight concurrency 
- valhalla - primitive types and specialized generics
- babylon -  heterogenous targets like  GPUs, SIMD, TPUs, FPGAs, execution engines like CUDA, SPIR-V, or Metal and domain-specific models (e.g., SQL, ORMs, ML/AI frameworks)
- maven, gradle 
** dhh vs rich hickey
|----------------+-------------------------------+---------------------------------|
| dimension      | dhh                           | rich hickey                     |
| :---           | :---                          | :---                            |
| primary goal   | developer joy & speed         | long-term clarity & correctness |
| software model | opinionated monolith (rails)  | composable libraries (clojure)  |
| code style     | expressive dsls & magic       | explicit data & pure functions  |
| philosophy     | convention over configuration | simple over easy                |
|----------------+-------------------------------+---------------------------------|
** clojure philosophy
- immutable data (facts - point of time snapshot, no side effects) vs ambassidors (objects) 
  - model information (complexity, correctness, reasoning)
  - simple and reproducible
  - reprsents what happned   
- state management - time (identity is seperate from state)
  - immutable by default; change is explicit, carefully managed exception, not the rule
  - loop > recur 
  - tail call optimization (tco) > trap > recur (won't grow stack) 
- extensiblity (beyond switch case) - better polymorphism 
  - monkey patching is recipe for chaos
  - functions live in own namespaces
  - composable, developer chooses
  - extends without modification
- pragmatic approach payoffs
  - simpler, more predictable programs
  - robust concurrency by default
  - composable and extensible systems 
  - avoids entire classes of bugs
  - focus on data manipulation
  - performance guarentees in semantics
** clojure reitit vs compojure
|-----------------+--------------------------------------------------------+----------------------------------------------|
| Feature         | Reitit                                                 | Compojure                                    |
| :---            | :---                                                   | :---                                         |
| Design          | Data-driven (routes are plain vectors/maps)            | Macro-based DSL (=GET=, =POST=, =defroutes=) |
| Performance     | Extremely fast (trie-based routing)                    | Moderate                                     |
| Platform        | Full-stack (JVM + ClojureScript/Frontend)              | Primarily Backend (JVM)                      |
| Coercion & Docs | Native support for Malli/Spec/Schema & Swagger/OpenAPI | Requires =compojure-api= extension           |
| Reverse Routing | Built-in (easy path generation from route names)       | Difficult / Limited                          |
|-----------------+--------------------------------------------------------+----------------------------------------------|

** clojure equivalent of ruby on rails
|------------------+-------------------------+----------------------|
| concept          | ruby / rails equivalent | clojure equivalent   |
|------------------+-------------------------+----------------------|
| language         | ruby                    | clojure              |
| http abstraction | rack                    | ring                 |
| routing          | rails router            | reitit or compojure  |
| database/orm     | activerecord            | next.jdbc + honeysql |
| html templating  | erb / slim              | hiccup or selmer     |
|------------------+-------------------------+----------------------|
** clojure equivalent of j2ee
|------------------------+-------------------------+---------------------------------------------|
| j2ee component         | clojure equivalent      | notes                                       |
| :---                   | :---                    | :---                                        |
| servlet / app server   | ring + jetty / http-kit | standard http abstraction                   |
| spring mvc / jax-rs    | reitit or compojure     | data-driven routing                         |
| cdi / ejb / spring ioc | integrant or component  | system lifecycle management                 |
| jpa / hibernate        | `next.jdbc` + honeysql  | clojure avoids orms, preferring sql-as-data |
| bean validation        | malli or spec           | schema and data validation                  |
| spring security        | buddy                   | authentication and authorization            |
|------------------------+-------------------------+---------------------------------------------|
** clojure equivalent of spring
*** Full Application Frameworks (Spring Boot equivalent)
- *[Kit](https://kit-clj.github.io/):* The modern standard for Clojure web apps (built on Integrant).
- *[Luminus](https://luminusweb.com/):* A popular, battle-tested micro-framework template.

*** The standard Clojure stack (Spring components broken down)
|------------------------------+----------------------------+----------------------------------------------------------------------|
| Spring Feature               | Clojure Equivalent         | Description                                                          |
| :---                         | :---                       | :---                                                                 |
| *IoC / Dependency Injection* | *Integrant* or *Component* | Data-driven lifecycle and dependency management.                     |
| *Spring MVC / REST*          | *Ring* + *Reitit*          | Ring handles HTTP abstractions; Reitit handles routing and coercion. |
| *Spring Data / JPA*          | *=next.jdbc=* + *HoneySQL* | Low-level JDBC wrapper + writing SQL as Clojure data structures.     |
| *Spring Security*            | *Buddy*                    | Authentication and authorization library.                            |
|------------------------------+----------------------------+----------------------------------------------------------------------|
** sqlite
sqlite is composed of several key components working together to process sql and access data:
- vdbe, pager (4096 bytes), wal/writer, fsync, db
- interface: handles connections, parses api calls, and passes them to the engine
- tokenizer & parser: converts sql text into a parse tree
- code generator (compiler): translates the parse tree into bytecode instructions
- virtual machine (vdbe): executes the bytecode. the heart of sqlite
- b-tree engine: manages data storage. tables and indices are stored as b-trees
- pager: manages memory, caching, and disk i/o, including acid transactions and rol- lbacia a journal/wal
- os interface (vfs): abstracts operating system calls for cross-platform file
** learn to build - agy
- build northstar crm with login, contacts, companies, deals, notes, search, and dashboard cards. use react, vite, typescript, firebase auth, firestore, and firebase hosting; plan first.
** genai code limitations 
- q: given that ai is generating most of the code, is it even useful to learn new languages deeply?
  - a: You should learn at least one programming language really well - like, really, really well - and know the specifics of others
    - for example, if you are building something in go for the first time, you can get far by prompting your way through it. you can ship working code without knowing the basics
    - but yes, you can read the code, but can you truly understand it? let me double down...
    - go has goroutines and channels. rust has ownership and lifetimes. these are not syntax details; they are mental models
    - ai can generate code in a language you do not know, but it cannot generate intuition for a language you have never understood
    - when a deadlock happens, memory usage balloons, or a race condition surfaces, you need that mental model to reason about it. ai cannot hand you that reasoning
    - according to me, what ai changes is the ramp-up time - more specifically, syntax friction. that cost is now close to zero. the remaining work is the interesting part: building intuition for how the language thinks
    - so, you do not need to memorize every standard library method in every language. but you do need to deeply understand at least one language and then pick up the specifics of others as needed
    - that depth in one language gives you the mental model to reason about all the others
    - as engineers, our job is to solve problems, not necessarily to write code
- source: https://x.com/arpit_bhayani/status/2065063532232835494?s=20
*** solution/s   
  - a senior architect knows that ai-generated code written by someone without intuition is a high-interest technical debt engine. it creates "franken-code"—syntactically flawless scripts that fail catastrophically under load because the underlying concurrency or memory topology was completely misunderstood
  - status quo: ai accelerates "time to first commit" but the "time to production-grade verification" remains static or widens if the developer lacks domain intuition
  - solution: master ability to read, reason and design domain models, system design constraints, execution models, failure topology and core computer science invariants (concurrency, memory, and types)
  - failure literacy: understand that each language has a failure topology. goroutine leaks for go. lifetime violations for rust. gc pressure for jvm languages. undefined behavior for c.
  - unchanged: first-principles frameworks, mece (mutually exclusive, collectively exhaustive) strategy, edge-case testing
***  build happy paths; learn from production incidents
- read rcas, outage reports and postmortems carefully to understand:
  - what changed recently?
  - what assumption failed?
  - which signal was missing?
  - which alert came too late?
  - what made the blast radius larger?
  - what guardrail was added later?
  - what process changed after this?    
*** core computer science invariants across languages
**** concurrency (time)
- the underlying truth across all the below five concurrency invariants exist because shared mutable state requires serialization of access that physical hardware does not provide automatically. every concurrency primitive — mutex, channel, actor, semaphore — is a mechanism for enforcing one or more of these invariants
  - mutual exclusion: at most one thread of execution may hold a lock on a shared resource at any given time. violation produces data races — two writers interleaving produces neither writer's intended state.
  - progress: if no thread holds a lock, some thread requesting it must eventually acquire it. violation produces livelock — threads are active but no work advances.
  - no starvation: every thread that requests a resource must eventually receive it. violation produces indefinite postponement — one thread monopolizes access while others wait forever.
  - happens-before: if operation a happens-before operation b, every thread that observes b must also observe a's effects. this is the invariant cpus and compilers violate via instruction reordering, and why memory barriers exist. violation produces visibility bugs — a write by thread 1 is not seen by thread 2 even after synchronization that looks correct.
  - deadlock freedom: no cycle can exist in the resource wait graph. if thread a waits for b's lock while b waits for a's lock, neither can proceed. the cycle is the violation.
**** memory (space)
- the underlying truth: memory invariants exist because hardware gives you a flat address space with no intrinsic notion of ownership, validity, or lifetime. everything below is enforced by the language runtime, the type system, the gc, or the programmer — never by the hardware itself.
  - validity: a pointer must refer to memory that has been allocated and not yet freed. violation: use-after-free, null dereference. the memory at that address now belongs to a different logical object or the os
  - single ownership (or controlled aliasing): at any point in time, either exactly one owner has write access, or multiple readers exist with no writer. both simultaneously violates the invariant. this is what rust's borrow checker enforces at compile time. languages without it push enforcement to runtime (gc) or abandon it (c)
  - bounds integrity: access to a buffer must stay within its allocated bounds. violation: buffer overflow. the most historically exploited class of memory bug because it corrupts adjacent memory — and in systems languages, that adjacent memory may be executable code or a return address
  - lifetime soundness: memory must not be accessed after its lifetime ends. a reference to stack memory that outlives its stack frame is a dangling reference — the address is valid in hardware but the logical object is gone
  - no aliased mutation: two pointers to the same memory location cannot both be used to mutate it concurrently or interleaved without synchronization. this bridges memory and concurrency — it's the invariant that makes data races a memory problem, not just a scheduling problem
**** type (meaning - logical interpretation)
- the underlying truth is that the types are propositions. a value inhabiting a type is a proof that the proposition holds. type checking is proof verification. unsoundness means the proof system can prove false propositions — which means you can derive anything, which means the type system provides no guarantees
  - type safety: a value of type t must never be used as if it were type s without an explicit, checked conversion. violation: type confusion bugs. in unsafe languages, casting a float's bit pattern and interpreting it as an integer is legal at the hardware level — the type invariant is what says it's wrong
  - soundness: if the type system says an expression has type t, evaluating that expression must produce a value of type t at runtime. a type system that allows proofs of type correctness that can fail at runtime is unsound. java's generic erasure and covariant arrays are classic soundness violations — both allow arraystoreexception at runtime for code that type-checks statically
  - progress and preservation (the two halves of type safety, formal definition): progress: a well-typed program either produces a value or takes a step — it does not get stuck. preservation: if an expression has type t and takes a step to expression e', then e' also has type t. together these guarantee well-typed programs cannot reach undefined states
  - exhaustiveness: a pattern match or case analysis over a sum type must handle all constructors. violation: a runtime match failure that the type system could have caught. haskell and rust enforce this. java switch historically did not
  - parametricity: a polymorphic function f : ∀a. a → a can only be the identity function. the type variable a is opaque — f has no operations available on a except to pass it through. this is a constraint, and that constraint is informative — it narrows the possible behaviors to one. this is the invariant that makes generic code more trustworthy than concrete code in well-typed languages
**** language level comparison 
|------------+---------------------------------+-----------------------------------------+-----------------------|
| language   | memory model (space)            | concurrency model (time)                | type system (meaning) |
|------------+---------------------------------+-----------------------------------------+-----------------------|
| c          | manual / explicit               | shared state (raw threads/mutexes)      | static / weak         |
| go         | automated (tracing gc)          | communicating sequential processes      | static / strong       |
| java       | automated (generational gc)     | shared state (threads/locks/virtual)    | static / strong       |
| clojure    | automated (tracing gc)          | epochal time model (immutability / stm) | dynamic / strong      |
| rust       | compile-time affine (lifetimes) | shared state (enforced by type safety)  | static / strong       |
| python     | automated (reference counting)  | global interpreter lock (gil) / async   | dynamic / strong      |
| javascript | automated (tracing gc)          | single-threaded event loop              | dynamic / weak        |
|------------+---------------------------------+-----------------------------------------+-----------------------|
* foundational programming concepts
- hashing – converting data into a fixed-size value using a hash function for fast lookup or comparison
- memoization – caching results of expensive function calls
- caching – storing data temporarily to serve faster on repeated access
- serialization: converting an object or data structure into a byte stream or string so it can be stored or transmitted
- deserialization – reconstructing the original object or data structure from that byte stream or string
- replication: copying data across multiple systems to improve availability and fault tolerance
- bit manipulation – using bitwise operations for compact/fast logic
- sharding – dividing large datasets into smaller chunks for efficiency
- divide & conquer – break, conquer recursively, merge
- atomicity: ensuring an operation either fully completes or doesn’t happen at all, with no partial changes
- load balancing: distributing workloads across multiple servers to optimize performance and avoid overload
- concurrency: running multiple tasks simultaneously, overlapping their execution to maximize efficiency
- garbage collection: automatically reclaiming memory by detecting and freeing unused objects
- mutex: a lock ensuring only one thread accesses a resource at a time to prevent conflicts
- event loop: a mechanism that handles asynchronous tasks by queuing and processing events in a single-threaded environment
- indexing: creating a lookup structure to quickly find data in a database or array
- rate limiting: restricting the number of requests a system handles in a given time to prevent overload
- checksum & crc – a value computed from data to verify its integrity after transfer or storage
- memory pooling – pre-allocating memory to reduce allocation overhead
- deadlock: a situation where two or more processes block each other, waiting for resources that the others hold
- lazy loading: delaying the initialization or loading of resources until they’re actually needed
- event sourcing: storing the state of a system as a sequence of events rather than a single snapshot
- race condition: when the outcome of a program depends on the unpredictable timing of multiple threads accessing shared resources
- mapreduce: a framework for processing large datasets by splitting them into smaller tasks (map) andcombining results (reduce)
- dependency injection: passing dependencies (like services or objects) into a component rather than having it create them internally
- fuzzing: testing software by throwing random, unexpected inputs at it to find bugs or vulnerabilities
* millenium problems
- the yang-mills "mass gap": if the fundamental forces of nature are carried by massless particles, why does the universe actually have weight? 🌌✨ #physics #math #millenniumprize
- the riemann hypothesis: a 160-year-old quest to prove that seemingly random prime numbers actually follow a perfectly ordered, deeply hidden rhythm. 🧩🔢 #math #riemannhypothesis
- the p vs. np problem: if it's incredibly easy to verify the correct answer to a puzzle, is there a hidden shortcut that makes it just as easy to solve from scratch? 🧩💡 #compsci #math #pvsnp
- the navier-stokes equations are the universe's master algorithm for fluid motion, dictating everything from chaotic global weather patterns to the swirling fluid simulations that power dynamic generative art. 🌊🎨🌪️ #fluiddynamics #physics #math
- the hodge conjecture: can the world’s most complex, smooth shapes always be broken down into simple, predictable geometric building blocks?  🧱📐 #math #hodgeconjecture #scienceexplained
- the poincaré conjecture: can you prove a shape is a sphere without seeing it from the outside?  yes—if you can shrink every loop on its surface to a point, it's a sphere. 🌐✨ #math #topology #scienceexplained
- birch and swinnerton-dyer (bsd) conjecture: it is math's ultimate shortcut - it claims a geometric curve's analytical "shadow" dropping to zero is absolute proof it holds infinite perfect coordinates 
* chronological intellectual evolution table - the staircase to knowledge
** western
|-----------------------------+----------------------------+-------------------------+--------------------|
| book name                   | author                     | place of origin         | time period        |
|-----------------------------+----------------------------+-------------------------+--------------------|
| the epic of gilgamesh       | anonymous (mesopotamian)   | ancient mesopotamia     | c. 2100 – 1200 bce |
| the republic                | plato                      | classical greece        | c. 375 bce         |
| diwan of al-mutanabbi       | al-mutanabbi               | abbasid caliphate       | 10th century ce    |
| risalat al-ghufran          | al-ma'arri                 | syria (abbasid era)     | c. 1033 ce         |
| the divine comedy           | dante alighieri            | florence, italy         | 1320 ce            |
| muqaddimah                  | ibn khaldun                | north africa (tunis)    | 1377 ce            |
| the prince                  | niccolò machiavelli        | florence, italy         | 1532 ce            |
| discourse on method         | rené descartes             | france                  | 1637 ce            |
| the critique of pure reason | immanuel kant              | prussia (germany)       | 1781 ce            |
| faust                       | johann wolfgang von goethe | germany                 | 1808 ce (part i)   |
| the brothers karamazov      | fyodor dostoevsky          | russia                  | 1880 ce            |
| thus spoke zarathustra      | friedrich nietzsche        | germany                 | 1883 ce            |
| les désorientés             | amin maalouf               | lebanon / france        | 2012 ce            |
| the meaning of relativity   | albert einstein            | germany / switzerland   | 1922 ce            |
| the prophet                 | kahlil gibran              | lebanon / united states | 1923 ce            |
| al-ayyam (the days)         | taha hussein               | egypt                   | 1929 ce            |
| a study of history          | arnold j. toynbee          | united kingdom          | 1934 – 1961 ce     |
| cosmos                      | carl sagan                 | united states           | 1980 ce            |
| a brief history of time     | stephen hawking            | united kingdom          | 1988 ce            |
| the road ahead              | bill gates                 | united states           | 1995 ce            |
|-----------------------------+----------------------------+-------------------------+--------------------|
** indian staircase of knowledge
|------------------------------+----------------------------+-------------------------+----------------------+-------------------------------------------|
| book / text name             | key author / mind          | region of origin        | time period          | philosophical & core curatorial domain    |
|------------------------------+----------------------------+-------------------------+----------------------+-------------------------------------------|
| the mahabharata              | attributed to vyasa        | upper gangetic plain    | c. 400 bce or older  | cosmic epic foundation; human nature      |
| the natyashastra             | bharata muni               | ancient india           | c. 200 bce – 200 ce  | aesthetic theory; rasa & codification     |
| the arthashastra             | chanakya (kautilya)        | taxila / pataliputra    | c. 2nd–3rd cent. bce | realpolitik, statecraft, economics        |
| aryabhatiya                  | aryabhata                  | kusumapura (patna)      | 499 ce               | astronomy; mathematical base (sine/0)     |
| the thirukkural              | thiruvalluvar              | tamilakam (south india) | c. 5th century ce    | ethics; granular daily conduct            |
| brahma-sphuta-siddhanta      | brahmagupta                | bhinmal (rajasthan)     | 628 ce               | algebra; operational rules of zero ($0$)  |
| abhigyanashakuntalam         | kalidasa                   | ujjain (malwa)          | c. 4th–5th cent. ce  | classical drama; memory and nature        |
| shunya sampadane             | veerashaiva vachana saints | karnataka               | 12th–15th cent. ce   | mystical dialectics; void as fullness     |
| yuktibhasa                   | jyesthadeva                | kerala school           | c. 1530 ce           | calculus foundations; infinite series     |
| gitanjali                    | rabindranath tagore        | bengal                  | 1910 ce              | universal humanism; lyrical devotion      |
| the annihilation of caste    | dr. b. r. ambedkar         | bombay / pan-india      | 1936 ce              | radical social equity; systemic critique  |
| the discovery of india       | jawaharlal nehru           | ahmednagar fort / india | 1946 ce              | historical synthesis; civilizational arc  |
| selected space vision papers | vikram sarabhai            | ahmedabad               | mid-20th century ce  | leapfrogging via technological vision     |
| cosmic ray & quantum papers  | homi j. bhabha             | mumbai                  | mid-20th century ce  | high-energy physics; macro infrastructure |
|------------------------------+----------------------------+-------------------------+----------------------+-------------------------------------------|
* type systems
** intro
- a type system is fundamentally about boundaries and predictability
  - abstraction level: replacing memory addresses with everyday metaphors (boxes, labels, contracts)
  - precision vs. intuition: balancing easy-to-grasp analogies with structural, mathematical realities
  - operational risk: shifting from "why does this matter?" to "how does this break at scale?"
** notes
- enforceable design contract (ieee standards)
  - space allocation and encoding
  - operational domain - set of valid operations (eg: arithmetic operations on numbers; concat for text)
* linus torvalds vs rich hickey
linus:  [hardware registers] -> [linear memory blocks] -> [imperative c code]
rich:   [real-world facts]   -> [immutable pure data]   -> [functional abstraction]
|----------------------+----------------------------------------------------------------------------------------------------------------+----------------------------------------------------------------------------------------------------------------------------------------+---------------------------------------------------|
| variable             | linus - imperative                                                                                             | rich -functional                                                                                                                       | notes                                             |
|----------------------+----------------------------------------------------------------------------------------------------------------+----------------------------------------------------------------------------------------------------------------------------------------+---------------------------------------------------|
| mastery              | infrastructure (os, db, embedded, game loop)                                                                   | architecture (complexity, distributed, massive)                                                                                        |                                                   |
| machine              | mechanical sympathy                                                                                            | information sympathy (domain model)                                                                                                    | cpu, registers, caches, and raw pointer addresses |
| state                | mutable slot                                                                                                   | immutable values                                                                                                                       |                                                   |
| system (simplicity)  | tactical transparency                                                                                          | structural independence                                                                                                                |                                                   |
| data layout          | arrays, flat contiguous structures, explicit buffers to maximize cpu cache hits                                | persistent trees, hashes, and vector matrices optimized for structural sharing                                                         |                                                   |
| concurrency model    | explicit mutexes, spinlocks, and manual thread sync. extremely high risk of race conditions, but zero overhead | software transactional memory (stm), agents, and atoms. lock-free, completely safe concurrency at the cost of runtime managed overhead |                                                   |
| primary failure mode | segmentation faults, memory leaks, security buffer overflows                                                   | garbage collection spikes, higher memory consumption, abstract stack traces                                                            |                                                   |
|----------------------+----------------------------------------------------------------------------------------------------------------+----------------------------------------------------------------------------------------------------------------------------------------+---------------------------------------------------|
* entropy - yt video - 3b1b
** compresssion = prediction | intelligence
- limits of compression
- correlation between data size & probablity 
- ascii - 8 bits / ch
- huffman - 4.2 bits / ch
- neural methods - 1.1 bits / ch
- shannon - information theory - machine learning is similar
  - prediction - next token prediciton
  - cross entropy loss
  - prediction and compression are mathematically equivalent 
- moon lander - up, down, left, right - instruction (code word)
- theoritical - head in the cloud 
|------------------+-----------+----------+-----------------------+-------------------------------------------------------------------------------------|
| student          | direction | encoding | rationale             | notes                                                                               |
|------------------+-----------+----------+-----------------------+-------------------------------------------------------------------------------------|
| straight forward | up        |       00 | most used (50%)       | 2 bits / instruction                                                                |
|                  | down      |       01 | next used (25%)       |                                                                                     |
|                  | left      |       10 | balance split (12.5%) |                                                                                     |
|                  | right     |       11 | - ditto -             |                                                                                     |
|------------------+-----------+----------+-----------------------+-------------------------------------------------------------------------------------|
| clever           | up        |        0 |                       | different bit sizes to optimize; decoding is diff since dividing line is not simple |
|                  | down      |       10 |                       | 1.75 bits / instruction                                                             |
|                  | left      |      110 |                       | prefix (free) code                                                                  |
|                  | right     |      111 |                       |                                                                                     |
|------------------+-----------+----------+-----------------------+-------------------------------------------------------------------------------------|
| theoritical      | up        |          |                       | random noise is incompressible                                                      |
|                  | down      |          |                       |                                                                                     |
|                  | left      |          |                       |                                                                                     |
|                  | right     |          |                       |                                                                                     |
|------------------+-----------+----------+-----------------------+-------------------------------------------------------------------------------------|
- part 2 & 3 of yt videos are yet to be released 

** ai infrastructure
- stage 1 — linux + networking - processes, memory, gpus, sockets, http, tcp/ip basics
- stage 2 — python + backend - async python, fastapi, queues, concurrency fundamentals
- stage 3 — gpu fundamentals - cuda basics, vram, batching, quantization, throughput
- stage 4 — llm inference -vllm, tensorrt-llm, speculative decoding, kv caching
- stage 5 — distributed systems -load balancing, queues, retries, autoscaling, distributed workers
- stage 6 — ai serving -model apis, streaming responses, rate limiting, observability
- stage 7 — data pipelines -kafka, airflow, etl pipelines, vector indexing
- stage 8 — kubernetes + cloud -docker, kubernetes, aws/gcp basics, infra automation
- stage 9 — monitoring + reliability -prometheus, grafana, tracing, ai cost monitoring
- stage 10 — real ai systems -deploy scalable chat apps, rag pipelines, inference clusters
- stage 11 — open source - contribute to inference tooling or ai infra projects
- stage 12 — apply - ai infra engineer, platform engineer, ml systems engineer
** softwar engineering
- api design - rest & grpc
- auth - oauth2, jwt, openid, passkeys
- databases - sql, nosql, sharding, indexing
- caching - redis, cdn, edge strategies
- event-driven systems - kafka, pulsar, streaming
- concurrency & async - reactive, structured concurrency
- distributed systems - microservices, service mesh
- security - https, encryption, zero trust, owasp
- observability - logs, tracing, metrics, otel
- cloud & deployment - docker, k8s, serverless
- infrastructure as code - terraform, pulumi
- ci/cd - pipelines, gitops, rollouts
- api gateways - rate limiting, routing, versioning
- testing - unit, integration, contract testing
- ai integration - llm apis, vector dbs, rag
* github outage
- https://x.com/pulkit_mittal_?s=20
- github’s recent outage is a real-world example of almost all of system design concepts:
1. autoscaling: scale capacity with demand automatically. github’s autoscaling missed the istio sidecar’s concurrency bottleneck.
2. retry storms: retries improve reliability until they amplify an outage.
3. load balancing: spread traffic across healthy capacity. haproxy nodes hit their flow limits.
4. failover: move traffic away from unhealthy infrastructure. github shifted traffic between regions, but the extra load created new problems.
5. backpressure: stop accepting work when the system is overloaded. otherwise, the queue keeps growing.
6. rate limiting: protect a service from too much traffic. github eventually blocked some copilot token requests to reduce load.
7. circuit breakers: stop hammering a dependency that is already failing.
8. cascading failures: one bottleneck -> more retries -> more traffic -> more failures.
this is why system design isn’t just about the happy path.








<eof>
