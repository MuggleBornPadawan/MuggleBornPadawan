;; my_first_program.scm
;; This is a comment - Scheme ignores lines starting with ;;

(display "Hello, Scheme World!") ; This prints a string to the console
(newline) ; This moves the cursor to the next line, like pressing Enter
